package com.example.myapplication.Stratego.GameActions;

/**
 * An interface for an action that can be given to the state regardless if it
 * is their turn or not
 */
public interface AnytimeAction {
    // an action only needs to implement this class
}
